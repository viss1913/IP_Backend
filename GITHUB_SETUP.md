# Инструкция по загрузке на GitHub и деплою на Railway

## 📦 Шаг 1: Инициализация Git и первый коммит

Выполните эти команды в терминале (в папке проекта):

```bash
# Инициализация git репозитория
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit: IP Backend CRM system"
```

## 🐙 Шаг 2: Создание репозитория на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните:
   - **Repository name:** `IP_Backend` (именно так, с подчёркиванием и заглавными буквами)
   - **Description:** `CRM система с AI интеграцией для управления заявками и агентами`
   - **Visibility:** выберите Public или Private
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (у нас уже всё есть)
4. Нажмите **"Create repository"**

## 🔗 Шаг 3: Подключение к GitHub и Push

После создания репозитория GitHub покажет инструкции. Выполните:

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/IP_Backend.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

**Пример:**
Если ваш GitHub username: `username`, то команда будет:
```bash
git remote add origin https://github.com/username/IP_Backend.git
git branch -M main
git push -u origin main
```

## 🚂 Шаг 4: Деплой на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub (если ещё не вошли)
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите ваш репозиторий `IP_Backend`
6. Railway автоматически определит Node.js проект и начнёт деплой

## ⚙️ Шаг 5: Настройка переменных окружения на Railway

После начала деплоя:

1. В Railway Dashboard → ваш проект → нажмите на сервис
2. Перейдите во вкладку **"Variables"**
3. Добавьте переменные (см. `RAILWAY_DEPLOY.md`):

### Обязательные переменные:

```env
PORT=3000
JWT_SECRET=ваш-случайный-секретный-ключ-минимум-32-символа
GEMINI_API_KEY=ваш_ключ_от_gemini
```

### Переменные для дефолтного агента (опционально):

```env
DEFAULT_AGENT_LOGIN=admin
DEFAULT_AGENT_PASSWORD=admin123
DEFAULT_AGENT_FIRST_NAME=Администратор
DEFAULT_AGENT_LAST_NAME=Системы
DEFAULT_AGENT_EMAIL=admin@example.com
DEFAULT_AGENT_PHONE=+7 900 000-00-00
DEFAULT_AGENT_CITY=Москва
```

## 🗄️ Шаг 6: Добавление MySQL базы данных

1. В Railway Dashboard → ваш проект → **"New"** → **"Database"** → **"MySQL"**
2. Railway создаст MySQL базу
3. В настройках MySQL базы найдите **"Variables"** и скопируйте:
   - `MYSQLHOST`
   - `MYSQLPORT`
   - `MYSQLUSER`
   - `MYSQLPASSWORD`
   - `MYSQLDATABASE`
4. Добавьте эти переменные в ваш основной сервис (не в базу, а в сервис приложения):
   - `DB_HOST` = значение из `MYSQLHOST`
   - `DB_PORT` = значение из `MYSQLPORT` (обычно 3306)
   - `DB_USER` = значение из `MYSQLUSER`
   - `DB_PASSWORD` = значение из `MYSQLPASSWORD`
   - `DB_NAME` = значение из `MYSQLDATABASE`

## ✅ Шаг 7: Проверка деплоя

1. После деплоя Railway предоставит URL вида: `https://your-project.up.railway.app`
2. Откройте этот URL в браузере
3. Вы увидите страницу входа
4. Войдите с данными по умолчанию:
   - **Логин:** `admin`
   - **Пароль:** `admin123`

## 🔍 Просмотр логов

Если что-то не работает:
1. Railway Dashboard → ваш проект → **"Deployments"**
2. Выберите последний деплой
3. Нажмите **"View Logs"**

## 📝 Полезные команды Git для будущего

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Получить обновления
git pull
```

## ⚠️ Важно!

1. **НЕ коммитьте файл `.env`** - он уже в `.gitignore`
2. **Измените пароль по умолчанию** после первого входа
3. **Измените JWT_SECRET** на случайный длинный ключ
4. **Проверьте логи** если что-то не работает

