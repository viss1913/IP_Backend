# ✅ Чеклист готовности к деплою на Railway

## ✅ Что уже готово:

- [x] Код залит на GitHub: https://github.com/viss1913/IP_Backend
- [x] package.json с правильным start script
- [x] .gitignore настроен (исключает .env, node_modules)
- [x] Статические файлы (public/) для веб-интерфейса
- [x] dotenv подключен для переменных окружения
- [x] PORT читается из process.env
- [x] railway.json создан для конфигурации

## 📋 Что нужно сделать на Railway:

### 1. Создать проект и подключить репозиторий
- [ ] Зайти на railway.app
- [ ] New Project → Deploy from GitHub repo
- [ ] Выбрать репозиторий `IP_Backend`

### 2. Добавить MySQL базу данных
- [ ] В проекте → New → Database → MySQL
- [ ] Railway создаст MySQL автоматически

### 3. Настроить переменные окружения

В настройках вашего сервиса (не базы!) → Variables добавить:

#### Обязательные:
```env
PORT=3000
JWT_SECRET=ваш-случайный-длинный-ключ-минимум-32-символа
GEMINI_API_KEY=ваш_ключ_от_gemini_api
```

#### Из MySQL базы (скопировать из Variables MySQL сервиса):
```env
DB_HOST=<значение из MYSQLHOST>
DB_PORT=<значение из MYSQLPORT> (обычно 3306)
DB_USER=<значение из MYSQLUSER>
DB_PASSWORD=<значение из MYSQLPASSWORD>
DB_NAME=<значение из MYSQLDATABASE>
```

#### Опциональные (для дефолтного агента):
```env
DEFAULT_AGENT_LOGIN=admin
DEFAULT_AGENT_PASSWORD=admin123
DEFAULT_AGENT_FIRST_NAME=Администратор
DEFAULT_AGENT_LAST_NAME=Системы
DEFAULT_AGENT_EMAIL=admin@example.com
DEFAULT_AGENT_PHONE=+7 900 000-00-00
DEFAULT_AGENT_CITY=Москва
```

### 4. Проверить деплой
- [ ] Railway автоматически начнёт деплой
- [ ] Дождаться завершения (зелёный статус)
- [ ] Получить URL приложения (Railway предоставит автоматически)
- [ ] Открыть URL в браузере
- [ ] Проверить страницу входа

## 🔍 Как проверить, что всё работает:

1. **Health check:**
   ```
   GET https://your-app.up.railway.app/health
   ```
   Должен вернуть: `{"status":"ok"}`

2. **Веб-интерфейс:**
   ```
   https://your-app.up.railway.app/
   ```
   Должна открыться страница входа

3. **API:**
   ```
   POST https://your-app.up.railway.app/api/auth/login
   Body: {"login":"admin","password":"admin123"}
   ```
   Должен вернуть токен

## ⚠️ Важные моменты:

1. **JWT_SECRET** - обязательно измените на случайный длинный ключ (минимум 32 символа)
2. **GEMINI_API_KEY** - нужен для работы AI функций
3. **MySQL переменные** - копируйте из Variables MySQL сервиса, не из самого сервиса приложения
4. **Пароль по умолчанию** - после первого входа обязательно смените через API

## 🐛 Troubleshooting:

### Ошибка подключения к БД:
- Проверьте, что MySQL сервис запущен
- Проверьте переменные DB_HOST, DB_USER, DB_PASSWORD
- Посмотрите логи в Railway Dashboard

### Ошибка при деплое:
- Проверьте логи в Railway → Deployments → View Logs
- Убедитесь, что все зависимости установлены
- Проверьте, что Node.js версия >= 18

### Страница не открывается:
- Проверьте, что PORT=3000 установлен
- Railway автоматически назначает порт, но лучше указать явно
- Проверьте, что деплой завершился успешно (зелёный статус)

## 📚 Полезные ссылки:

- [Railway Docs](https://docs.railway.app)
- Ваш репозиторий: https://github.com/viss1913/IP_Backend

