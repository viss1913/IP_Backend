# Инструкция по деплою на Railway

## 🚀 Быстрый старт

### 1. Подготовка проекта

1. Убедитесь, что у вас есть аккаунт на [Railway](https://railway.app)
2. Установите Railway CLI (опционально):
   ```bash
   npm i -g @railway/cli
   ```

### 2. Деплой через Railway Dashboard

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите "New Project"
3. Выберите "Deploy from GitHub repo" (если проект в GitHub) или "Empty Project"
4. Если пустой проект:
   - Нажмите "New" → "GitHub Repo"
   - Выберите ваш репозиторий
   - Или подключите локальный проект через CLI

### 3. Настройка переменных окружения

В Railway Dashboard → ваш проект → Variables добавьте:

```env
# MySQL Database (Railway предоставит свои значения)
DB_HOST=your-railway-mysql-host
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your-railway-mysql-password
DB_NAME=railway

# Server
PORT=3000

# Gemini AI
GEMINI_API_KEY=your_gemini_api_key

# JWT
JWT_SECRET=your-very-secret-jwt-key-change-this
JWT_EXPIRES_IN=7d

# Дефолтный агент (опционально)
DEFAULT_AGENT_LOGIN=admin
DEFAULT_AGENT_PASSWORD=admin123
DEFAULT_AGENT_FIRST_NAME=Администратор
DEFAULT_AGENT_LAST_NAME=Системы
DEFAULT_AGENT_EMAIL=admin@example.com
DEFAULT_AGENT_PHONE=+7 900 000-00-00
DEFAULT_AGENT_CITY=Москва
```

### 4. Настройка MySQL на Railway

1. В Railway Dashboard → ваш проект → "New" → "Database" → "MySQL"
2. Railway автоматически создаст MySQL базу
3. Скопируйте переменные окружения из базы данных в Variables проекта

### 5. Настройка сборки

Railway автоматически определит Node.js проект. Убедитесь, что в `package.json` есть:

```json
{
  "scripts": {
    "start": "ts-node src/server.ts"
  }
}
```

### 6. Получение URL вашего приложения

После деплоя Railway предоставит URL вида:
- `https://your-project-name.up.railway.app`

## 🔐 Как попасть в админку после деплоя

### Вариант 1: Через веб-интерфейс (рекомендуется)

1. Откройте URL вашего приложения: `https://your-project-name.up.railway.app`
2. Вы увидите страницу входа
3. Введите данные по умолчанию:
   - **Логин:** `admin`
   - **Пароль:** `admin123`
4. После входа вы попадёте в админку

### Вариант 2: Через API напрямую

1. Получите токен:
   ```bash
   curl -X POST https://your-project-name.up.railway.app/api/auth/login \
     -H "Content-Type: application/json" \
     -d '{"login":"admin","password":"admin123"}'
   ```

2. Используйте полученный токен для всех запросов:
   ```bash
   curl https://your-project-name.up.railway.app/api/auth/me \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

## 📝 Важные моменты

1. **Измените пароль по умолчанию!** После первого входа обязательно смените пароль через API:
   ```bash
   PATCH /api/agents/00000000-0000-0000-0000-000000000001
   {
     "password": "новый_безопасный_пароль"
   }
   ```

2. **Измените JWT_SECRET** на случайный длинный ключ в продакшене

3. **Проверьте переменные окружения** - особенно настройки MySQL

4. **Логи** можно посмотреть в Railway Dashboard → Deployments → View Logs

## 🛠️ Troubleshooting

### Проблема: База данных не подключается
- Проверьте переменные окружения DB_HOST, DB_USER, DB_PASSWORD
- Убедитесь, что MySQL сервис запущен в Railway

### Проблема: Страница не открывается
- Проверьте, что порт установлен в переменной PORT=3000
- Railway автоматически назначает порт, но лучше указать явно

### Проблема: Ошибка при создании дефолтного агента
- Проверьте логи в Railway Dashboard
- Убедитесь, что таблицы созданы (они создаются автоматически)

## 📚 Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)

